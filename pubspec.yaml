name: delta_bypass
description: "Delta Bypass - MIUI style bypass tool built with flutter_miuix"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_miuix: ^1.2.0
  connectivity_plus: ^7.3.1
  device_info_plus: ^13.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true